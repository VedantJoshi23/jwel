NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Optional. Absent means Sentry.init runs with no DSN, which is a no-op — see
# instrumentation-client.ts. A DSN is public by design (write-only).
# NEXT_PUBLIC_SENTRY_DSN=
