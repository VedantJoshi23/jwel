apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Service name on jwel-net, not localhost — Grafana and Prometheus are
    # separate containers.
    url: http://prometheus:9090
    isDefault: true
    editable: false
